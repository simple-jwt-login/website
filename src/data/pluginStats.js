// Single source of truth for plugin stats shown across the site
// (homepage hero, donate page, JSON-LD). Update here, not per-page.
export const ACTIVE_INSTALLS = 4000;
export const TOTAL_DOWNLOADS = 87000;
export const RATING = 5;
export const BEST_RATING = 5;
export const RATING_COUNT = 46;
export const ACTIVE_YEARS = 6;

export const formatNumber = (n) => n.toLocaleString('en-US');
