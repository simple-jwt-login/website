type: object
required:
  - email
properties:
  email:
    type: string
    example: contact@simplejwtlogin.com
    description: Email address of the account to reset.
  AUTH_KEY:
    type: string
    example: "MY_SUPER_SECRET_AUTH_KEY"
    description: Required when "Reset Password Requires Auth Code" is enabled.
